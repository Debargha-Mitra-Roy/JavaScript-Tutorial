/* Use logical operators to find whether the age of a person lies between 10 to 20. */

let age = prompt("Enter the age : ")

if (age >= 10 && age <= 20)
    console.log("Age lies between 10 to 20.");

else
    console.log("Age does not lies betweeen 10 to 20.");