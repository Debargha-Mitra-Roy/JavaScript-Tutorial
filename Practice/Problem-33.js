/* Change the background of the page to yellow, red or any oter color based on user input through prompt. */

let color = prompt("Enter the page background color : ");
document.body.style.background = color;