/* Try to add a new key to the constant object in Problem-03. Were you able to do it. */

const a = {
    name: "Ryzen",
    roll: 10,
    isPass: true
};

console.log(a.name, a.roll, a.isTrue);

a['age'] = 20;
a['name'] = "Debargha"; // Change in runtime

console.log(a);

// Yes, we can do it.