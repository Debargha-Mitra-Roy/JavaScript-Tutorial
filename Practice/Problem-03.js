/* Create a constant object in JavaScript. Can you change it to hold a number later. */

const a = {
    name: "Ryzen",
    roll: 10,
    isPass: true
};

console.log(a);

// a = 10; // Throwback an error

// 'const' can not hold any other datatypes later.